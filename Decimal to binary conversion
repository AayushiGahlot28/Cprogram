// DECIMAL TO BINARY//
int main()
{
    int decimal,i,sub,rem,a[100];
    printf("enter decimal num");
    scanf("%d",&decimal);
    for(i=0;decimal!=0;i++)
    {
        a[i]=decimal%2;
        decimal=decimal/2;
        
    }
    for(i = i - 1; i >= 0; i--)
{
    printf("%d", a[i]);  
}

}
